# Handle line endings automatically for files detected as text 
# but do not force a specific style on the local working tree.
* text=auto

# Keep specific overrides only for files that MUST have a certain format to run.
/gradlew text eol=lf
*.bat text eol=crlf